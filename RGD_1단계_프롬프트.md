# 1단계 프롬프트 — RGD 물리 코어 + 검증

> Claude Code(VS Code Superpowers)에 그대로 붙여넣어 사용한다.
> UI는 이 단계에서 만들지 않는다. verify.js가 모두 PASS한 뒤 2단계로 넘어간다.

---

## 작업 목표

논문용 시뮬레이션의 물리 코어를 Node.js로 구현하고, 해석해와 대조해 검증한다.
UI는 이 단계에서 만들지 않는다.

목적: 파장이 입자 크기보다 훨씬 긴 영역(산란/레일리)과 파장이 입자 크기와 비슷한 영역(회절) 사이의 경계를 수치로 정하는 것. 논문의 통합 변수는 λ/a이다.

---

## 물리 모형: Rayleigh–Gans–Debye (RGD) 근사

표준 RGD를 사용한다. 임의로 다른 모형을 쓰지 말 것.

### 규약과 고정값

- 입사파 +z 진행, 전기장 x축 편광, 시간 규약 e^{-iωt}
- 산란체: 반지름 a인 구. **a = 1 고정**
- **λ가 조작 변인**이며, a=1이므로 λ 값이 그대로 λ/a 이다
- k = 2π/λ,  크기 매개변수 x = ka = 2π/λ
- E₀ = 1
- **총 분극률 α_total = 1 고정. 개별 쌍극자는 α = α_total/N = 1/N**

> 중요: 개별 α를 1로 고정하면 안 된다. λ가 바뀌면 N이 바뀌므로 총 분극률이 격자 해상도에 따라 변해 λ⁻⁴ 기울기가 오염된다.

α를 x에 무관한 상수로 두는 물리적 근거를 코드 주석에 남길 것:
ω ≪ ω₀ 영역에서는 복원력이 지배하여 변위 진폭이 거의 일정하고, 따라서 분극률도 거의 일정하다. 이 시뮬레이션은 분광학적 축(ω/ω₀)이 아니라 기하학적 축(2πa/λ)만을 다룬다.

### 각 쌍극자

```
p_j = α · E₀ · x̂ · exp(i k z_j)
```

### 원거리 산란파

```
E_sca(n̂) ∝ (k²αE₀ / r) · sinΘ · F(q)
```

- Θ: n̂와 편광축 x̂ 사이의 각. 편광 인자 sin²Θ = 1 − (n̂·x̂)²
- F(q) = Σ_j exp(i q·r_j),  q = k(ẑ − n̂),  |q| = 2k·sin(θ/2)
- dσ/dΩ ∝ sin²Θ · |F(q)|²

### φ 적분은 해석적으로 처리한다

구형 산란체에서 |F|는 |q|에만 의존하므로 θ만의 함수다.
φ 의존성은 편광 인자에만 남고 해석적으로 적분된다:

```
∫₀^{2π} (1 − sin²θ·cos²φ) dφ = π(1 + cos²θ)
```

따라서 상용 경로는 1차원 적분:

```
σ = (8π/3)·k⁴·α_total²·G(x)

G(x) = (3/8)·∫_{−1}^{1} (1 + μ²)·|F(μ)/N|² dμ,   μ = cosθ
```

- G(x)가 이 시뮬레이션의 핵심 단일 지표다. G(0) = 1 이고, G가 1에서 벗어나는 정도가 곧 레일리 근사의 이탈량이다.
- μ에 대해 Gauss–Legendre. 노드/가중치는 Newton법으로 순수 JS 구현
- 기본 32점. 64점 배증 시 상대 변화를 verify에서 출력할 것.
  피적분 함수는 μ의 해석 함수이므로 지수적으로 수렴한다. 수렴이 느리면 코드에 버그가 있는 것이다.

---

## 이산화

- 배치: 반지름 a 안에 들어오는 격자점만 취하는 정육면체 격자 (난수 배치 금지)
- **h가 아니라 n_d(지름당 격자점 수)를 정수로 지정한다.** h = 2a/n_d, N은 종속 변수
- 제약: k·h ≤ 0.3 ⟺ n_d ≥ 20x/3. 또한 기하학적 충실도를 위해 n_d ≥ 20
  → `n_d = max(20, ceil(20x/3))` 을 자동 적용
- x ≤ 2 범위에서는 n_d = 20 (N ≈ 4200) 이면 충분하다

---

## 검증 (verify.js)

균일 구의 형상 인자 해석해:

```
F(q)/N → f(u) = 3(sin u − u·cos u)/u³,   u = qa = 2x·sin(θ/2)
```

(u→0에서 f→1. 급수 전개로 0/0 회피할 것)

다음 5종을 모두 확인하고, 각각 임계값과 PASS/FAIL을 함께 출력한다.

### 1. 형상 인자 수렴

x = 0.1, 0.5, 1.0, 2.0 각각 θ = 0°~180°에서 |F|/N vs f(u) 최대 상대 오차
임계값: 1% 이내

### 2. 전방 산란 불변

θ=0에서 q=0 이므로 x와 무관하게 F/N = 1 이어야 한다.
어긋나면 q의 부호 또는 시간 규약이 틀린 것이다. 임계값: 1e-12

### 3. 레일리 극한 — 기울기와 계수를 모두 확인

- log σ vs log(1/λ) 기울기가 4.00에 수렴하는가 (임계값: |기울기−4| < 0.01)
- **계수까지 확인**: x→0 에서 G(x) → 1.000 인가 (임계값: 1e-3)
  (|F/N| = 1 이면 ∫(1+μ²)dμ = 8/3, 즉 계수 8π/3)

기울기만 보는 것보다 훨씬 민감한 지표다. 편광 인자, 입체각 요소, 규격화 중 하나라도 어긋나면 여기서 드러난다.

### 4. N 비의존성

α_total을 고정했으므로 **σ는 N에 무관해야 한다.**
n_d = 20, 28, 40, 56 에 대해 같은 x에서 σ를 계산하고 상대 편차 출력.
임계값: 0.5% 이내.

(n_d를 정확히 배수로 맞출 필요 없다. 정육면체 격자를 구로 자르면 N이 계단식으로 뛰므로 무의미하다.)

### 5. 격자 이방성 (2D 적분 대조)

1차원 축약은 F의 등방성을 가정하므로 정육면체 격자가 깨뜨리는 구대칭을 감지하지 못한다. 검증용으로만 완전한 2D 적분을 별도 구현:

- θ: Gauss–Legendre (cosθ에 대해)
- φ: 균일 격자. 피적분 함수가 차수 2의 삼각다항식이므로 8점이면 기계 정밀도로 정확하다. 그 이상 쓰지 말 것

`|σ_2D − σ_1D| / σ_1D` 출력. 임계값 0.5%. 초과 시 n_d를 늘리라는 경고.

---

## 물리 프리셋 (검증 후 λ 훑기에서 사용)

성간 티끌 a = 0.1 μm 기준:

| 전자기파 | λ/a | x | 영역 |
|---|---|---|---|
| 21cm 전파 | 2.1e6 | 3.0e-6 | 완전한 레일리 영역 |
| 가시광선 500nm | 5 | 1.26 | 이미 레일리를 벗어난 영역 |

λ 훑기 기본 범위: λ/a ∈ [3.14, 1e3] 로그 간격 (x ∈ [0.0063, 2]).
21cm 프리셋은 별도 단일 점으로 계산.

---

## 모형의 한계 (주석 + README에 반드시 명시)

- **Born 근사**: 쌍극자 간 상호작용을 무시한다
- **RGD 적용 조건**: |m − 1| ≪ 1 이고 2x|m − 1| ≪ 1.
  실제 규산염 성간 티끌은 m ≈ 1.7 이므로 **정량적으로는 맞지 않는다.**
  이 시뮬레이션의 목적은 정량적 예측이 아니라 경향과 기제의 확인이다.
- 그림자 효과와 흡수가 없으므로 x ≳ 3 에서는 물리적으로 신뢰할 수 없다. x ≤ 2 로 제한할 것.
- 이 모형은 기하학적 축(2πa/λ)만 다룬다. 분광학적 축(ω/ω₀)은 다루지 않는다.

---

## 출력 형식 (verify.js)

- `console.table` 로 섹션별 표. 각 행에 [측정값, 임계값, PASS/FAIL] 포함
- 모든 항목 통과 여부를 마지막에 요약 한 줄로
- 실패 시 `process.exit(1)`
- `verify-results.json` 저장 (파라미터 헤더 포함, 재현성 확보)
- λ 훑기 결과는 `sweep.csv` 로도 저장 (논문 표에 사용)

---

## 모듈 방식

- `physics.js`는 IIFE로 감싸 `RGD` 단일 네임스페이스만 노출
- 파일 맨 아래에 조건부 export:

```javascript
if (typeof module !== 'undefined' && module.exports) module.exports = RGD;
```

- `physics.js`에는 DOM API도, Node 전용 API(fs, process 등)도 넣지 말 것
- `verify.js`에서만 console/fs/process 사용
- `package.json`은 만들지 않는다

---

## 산출물

| 파일 | 내용 |
|---|---|
| `physics.js` | 순수 계산 함수 |
| `verify.js` | 위 검증 5종 실행 |
| `DESIGN.md` | 규약, 고정값, 적분 방식, 이산화 방식 등 확정된 설계 결정 기록 |
| `README.md` | 모형, 한계, 검증 결과 요약 |

---

## 구현 순서

1D/2D 모두 소박한 N-항 합으로 먼저 구현하고 검증을 통과시킨다.
성능 최적화(z 층별 히스토그램 등)는 그 다음이며, 최적화 전후 결과가 일치하는지 대조할 것.

**verify.js가 모두 PASS하기 전에는 다음 단계로 넘어가지 말 것.**

---

## 부록 — 이미 확정된 설계 결정 (DESIGN.md에 옮겨 기록할 것)

세션이 바뀌어도 다시 묻지 않도록, 아래 결정 사항을 `DESIGN.md`에 그대로 남긴다.

1. **모듈 방식**: CommonJS + 조건부 export (IIFE 네임스페이스 `RGD`). package.json 없음. ESM 사용하지 않음.
2. **구면 적분**: Gauss–Legendre(μ = cosθ) 1차원. φ는 해석적으로 처리. verify에서만 2D 대조.
3. **무차원화**: a = 1 고정, λ가 조작 변인. α_total = 1 고정(개별 α = 1/N). E₀ = 1.
4. **이산화 파라미터**: h가 아니라 n_d(지름당 격자점 수)를 정수로 지정.
5. **핵심 지표**: G(x). G(0) = 1이며, 1에서의 이탈량이 레일리 근사의 붕괴 정도를 나타낸다.
