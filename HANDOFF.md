# HANDOFF — RGD 소입자 산란 시뮬레이션

갱신: 2026-07-27 · 기준 커밋 `ddc5b3a` (브랜치 `feature/waveguide-fdtd`)

> 다음 세션이 이 문서 하나만 읽고 이어받을 수 있게 쓴 인수인계 문서입니다.
> **§6(불변 조건)**과 **§8(다음 단계)**이 핵심입니다.

---

## 1. 한 줄 요약

Rayleigh–Gans–Debye 근사로 소입자 산란을 계산하는 **물리 코어가 완성·검증 완료**(13항목)이고,
그 위에 **입사파 · 산란파 · 합을 세로 3단으로 쌓아 애니메이션으로 보여 주는 탐색 화면**이 올라가 있습니다.
UI도 자동 회귀 테스트(13항목)로 검증됩니다.

전달하려는 한 문장:

> **파장이 길어지면 가운데 줄(산란파)이 사라지고, 그래서 아래 줄(합)이 위 줄(입사파)과 구별되지 않는다.**

---

## 2. 파일 지도

| 파일 | 크기 | 역할 |
|---|---|---|
| `physics.js` | 24 KB | 물리 코어. 브라우저·Node 양쪽에서 쓰는 `RGD` 전역 IIFE. **DOM 의존 없음** |
| `verify.js` | 27 KB | 물리 검증 13항목 + 회귀 체크. `console`/`fs`/`process`는 **여기서만** |
| `verify-ui.js` | 8 KB | **화면 회귀 13항목** (Playwright). 축척·표식 크기·모드 복원 |
| `index.html` | 16 KB | 패널 마크업 + ⓘ 설명 오버레이 본문 |
| `style.css` | 13 KB | 스크롤 없는 한 화면 레이아웃(`html,body{overflow:hidden}`) |
| `script.js` | 63 KB | 렌더링·애니메이션·상호작용 전부. 단일 IIFE, ES 모듈 아님 |
| `sweep.csv` | 5 KB | **논문 표 원본 데이터. 재생성만 하고 손으로 고치지 않음** |
| `verify-results.json` | 3 KB | 물리 검증 결과 (`generatedAt` 타임스탬프만 매 실행 갱신) |
| `package.json` | | 검증 스크립트 + playwright(devDependencies 전용) |
| `.gitignore` | | `node_modules/`, `package-lock.json` |
| `DESIGN.md` / `README.md` | | 1단계 설계·개요 |
| `docs/superpowers/specs/2026-07-25-rgd-scattering-design.md` | | 1단계(물리 코어) 설계 전문 |
| `docs/superpowers/specs/2026-07-26-nearfield-panels-design.md` | | 2단계(근접장 패널) 설계 전문 |
| `RGD_1단계_프롬프트.md` / `RGD_2단계_프롬프트.md` | | 원 사양서 |

로딩 방식: `<link>` + 클래식 `<script src>`. **`index.html`을 더블클릭하면 그대로 열립니다**
(ES 모듈·`fetch` 미사용). `package.json`은 검증용일 뿐 페이지 로딩 경로에 관여하지 않습니다.

---

## 3. 검증 두 갈래

역할이 겹치지 않게 분리되어 있습니다.

```
npm run test:physics    node verify.js       의존성 없음, 그대로 실행 가능
npm run test:ui         node verify-ui.js    playwright 필요
npm run test:ui:setup   Chromium 내려받기, 최초 1회
```

### `verify.js` — 물리 코어 13항목 (전부 PASS)

| # | 항목 | 측정 | 임계 |
|---|---|---|---|
| 1 | 형상 인자 수렴 (절대오차, θ 181점) | 2.69e-3 | 0.01 |
| 2 | 전방 산란 불변 (θ=0, F/N=1) | 0 | 1e-12 |
| 3 | 레일리 극한 — 기울기 | 3.999619 | \|기울기−4\| < 0.01 |
| 3′ | 레일리 극한 — 계수 (21cm, G→1) | G = 1.000000000 | \|G−1\| < 1e-3 |
| 4 | N 비의존성 (x=2.0, n_d=20/28/40/56) | 0.2376% | 0.5% |
| 5a | 격자 이방성 — 2D 대조 | 0.0280% | 0.5% |
| 5b | 격자 이방성 — φ=0 vs φ=45° | 0.1268% | 0.5% |
| 6 | 격자 중심 확인 (Im(F)/N ≈ 0) | 4.71e-16 | 1e-12 |
| 7 | 원거리 한계 (근접장 vs dσ/dΩ) | 2.55e-3 | 1e-2 |
| 8a | 정전기 극한 (kR=1e-4) | 4.46e-16 | 1e-12 |
| 8b | 유도항 k 스케일링 \|ind\|/\|sta\| = kR | 2.71e-16 | 1e-12 |
| 9a | y 대칭 최적화 vs 브루트포스 | 3.38e-15 | 1e-10 |
| 9b | x 거울 관계 (E_x 짝 / E_z 홀) | 2.37e-15 | 1e-10 |
| nd | sweep 전 구간 n_d 상수성 (계단 방지 회귀) | 상수(20) | 상수(20) |

`verify.js`는 **a = 1, α_total = 1** 로 돕니다. 화면 쪽이 실단위로 바뀐 것과 무관하게
이 값으로 계속 돌려야 위 수치가 재현됩니다.

### `verify-ui.js` — 화면 회귀 13항목 (전부 PASS)

DOM 없는 `verify.js`로는 잴 수 없는 것들만 담습니다.

- 줄무늬 주기가 λ에 비례 (λ/a = 3.19 / 5.01 / 40.09, 편차 0 ~ −2.1%, 허용 ±5%)
- 물체 표식 지름 ≤ 패널 높이 20% (실측 19.7%), 세 λ 모두 45px로 동일
- 물리 판독값(Q · G · \|E_산란\|/\|E_입사\|)이 축척 모드와 무관 (문자 단위 일치)
- `SCALE_MODE='lambda'` 복원 시 입사파 줄이 세 λ에서 픽셀 동일
- 세로 스크롤 없음 · 콘솔 에러 0건

줄무늬 주기는 **영교차 간격의 중앙값 × 2**로 잽니다. 마루를 세는 방식은 λ/a = 40에서
한 주기가 패널 폭의 40%라 실패합니다.

---

## 4. 물리 코어 (`physics.js`) — 공개 API

```
chooseNd, buildDipoleGrid,
qVector, formFactor, formFactorPhi0Columns, analyticShapeFactor,
gaussLegendreNodes, dSigmaDOmega, sigma1D, sigma2D,
dipoleFieldKernel, scatteredFieldAt, scatteredFieldXZPlane
```

- **원거리 경로**: `formFactor` → `dSigmaDOmega` → `sigma1D`/`sigma2D` → σ, G
  - `σ = (8π/3)·k⁴·α_total²·G(x)`
- **근접장 경로**: Jackson 9.18 완전 쌍극자 장을 **복사(1/R, k²) · 유도(1/R², k¹) · 정전(1/R³, k⁰)** 으로 분해
  - `dipoleFieldKernel` — 쌍극자 하나
  - `scatteredFieldAt` — 브루트포스 격자 합 (검증 기준)
  - `scatteredFieldXZPlane` — y 대칭 최적화 (**화면이 실제로 쓰는 것**)
- 시간 규약 `e^{-iωt}`, `E0 = 1`

**스케일 공변성** — 격자를 `a`배로 늘리면 `k`는 `1/a`배가 되고 `α ∝ a³`이므로 세 항에서
`a`가 정확히 상쇄됩니다. 즉 **λ/a 가 같으면 그림이 같습니다.** "λ/a 고정" 체크박스가
그것을 보여 주고, 픽셀 단위 동일함을 확인했습니다.

---

## 5. 화면 구조

탭은 없습니다. 위에서 아래로 한 화면입니다.

```
┌─ 컨트롤 (2줄 고정) ────────────────────────────────────────────┐
│ 1줄: a 슬라이더 · λ 슬라이더 · λ/a 고정 · 프리셋 2 · 상태 배지  │
│ 2줄: 숫자 판독 · 재생/속도 · 화살표 · 화면 축척 · 배율 · 표시 항 │
└────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────┬──────────────────┐
│ 입사파                                │                  │
├──────────────────────────────────────┤ 내부 전하의 진동 │
│ 산란파                                │ (3단 전체 높이)  │
├──────────────────────────────────────┤                  │
│ 합                                    │                  │
└──────────────────────────────────────┴──────────────────┘
▸ 상세 보기 — 산란파는 어느 방향으로 얼마나 가는가   ← <details>, 기본 접힘
     펼치면 340px 고정: 산란 패턴(좌) · 위상자 합(우)
```

### 알파벳 라벨은 전부 제거했습니다
제목·캡션·ⓘ 제목·본문 상호 참조·오프스크린 캐시 key 모두 내용 기반입니다
(`paint('incident'/'scattered'/'total')`). 이 문서에서도 (A)~(F)를 쓰지 않습니다.

### 화면 축척 — `SCALE_MODE`
| 모드 | 동작 | 쓸 때 |
|---|---|---|
| `'a'` **(기본)** | x 반범위 = `SCALE_A_HALF`(= 8a) 고정. 화면이 λ와 무관한 **고정된 자** | 파장이 길어지는 것을 보여줄 때 |
| `'lambda'` | z 범위 = 3λ. λ를 바꿔도 입사파 줄이 픽셀 단위로 동일 | 변하는 것이 산란파 하나뿐인 비교를 할 때 |

`SCALE_A_HALF = 8`은 임의값이 아니라 **수용 기준이 강제한 값**입니다:
물체 지름 3a ≤ 패널 높이(2·X_HALF)의 20% → **X_HALF ≥ 7.5**. 8이면 19.7%입니다.
후보였던 2.5로 두면 물체가 패널 높이의 60%를 차지해 위아래로 잘립니다.

### 애니메이션
- 복소 장 캐싱: `fieldRe`/`fieldIm` × {radiation, induction, static, **full**}, `incidentRe`/`incidentIm`
- 프레임마다 하는 일은 `val = Re·cos(ωt) + Im·sin(ωt)` 곱셈 두 번뿐.
  무거운 쌍극자 합은 **a 또는 λ가 바뀔 때만** 돕니다.
- **화면 주기 2초 고정**(`SCREEN_PERIOD`). 실제 ω를 쓰면 21cm에서는 멈춰 보이고
  가시광선에서는 폭주합니다.
- 실측: 60fps, λ 변경 시 재계산 0.5초(슬라이더를 놓을 때만).
- 내부 전하의 진동 패널도 `cos(k·z − ωt)`로 같은 위상에 동기됩니다.

### 축 위 화살표 (기본 켬)
- **색 지도와 같은 격자(`ND_DISPLAY = 14`)** 로 계산합니다. 물리 격자로 계산하면
  밑에 깔린 색과 값이 미세하게 어긋나 겹쳐 보였을 때 어색해집니다.
- 화면 x 표본이 셀 중심이라 x=0 표본이 없으므로, 격자에서 읽지 않고
  `scatteredFieldXZPlane`을 x=0에서 직접 부릅니다(40점, `|z| < 1.5a` 생략).
- 세 줄 공통 스케일(`|E_입사| = 1`이 행 높이의 35%). 산란파 줄만 표시 배율을 함께 겁니다.

### 산란파 배율
기본 **×1**입니다. 자동 배율이면 λ를 늘려도 늘 비슷하게 보여 "약해진다"는 메시지가 사라집니다.
백지로 보일 때는 배지로 알리는데, 기준은 화면 최댓값이 아니라 **z = λ 지점의 비**입니다 —
화면 최댓값은 마스크 가장자리 1/R³ 근접장이 늘 0.1 수준이라 λ와 거의 무관해 백지 여부를
전혀 가려내지 못합니다. 합 패널에는 배율을 **절대** 적용하지 않습니다.

---

## 6. 절대 깨면 안 되는 것

1. **`sweep.csv`는 한 글자도 바뀌면 안 됨** — 논문 표 원본. 가시광선 행
   `G = 0.5563911380033072` (63행). `verify.js`가 재생성하므로
   **커밋 전 반드시 `git status`로 diff 없음을 확인**
2. **σ/G 계산 경로(`sigma1D`/`sigma2D`, nd=20)는 건드리지 않음**
3. **`npm run test:physics` 13항목 PASS 유지** — 그리고 `verify.js`는 계속
   **a = 1, α_total = 1** 로 돌린다(화면이 실단위인 것과 무관)
4. ~~a = 1 고정~~ → **해제됨.** a와 λ가 독립 슬라이더이고 `α_total = 0.39·a³`
5. **마스크 반경 1.5a** (물리). 화면 표식 크기는 별개로 상한을 둔다
6. **더블클릭으로 열리는 구조** — ES 모듈·`fetch` 금지. playwright는
   devDependencies 전용이라 어떤 산출물에도 들어가지 않는다
7. **UI에 긴 설명문 추가 금지** — 캡션은 패널당 한 줄 이내. 긴 설명은 ⓘ 오버레이로
8. **컨트롤 줄 높이는 고정** — 줄바꿈을 허용하면 판독창 글자 수가 바뀔 때
   (`"500 nm"` → `"1.00 μm"`) 패널 높이가 따라 변한다. 실제로 났던 버그다

---

## 7. 조정 상수 한눈에 (`script.js` 상단)

| 상수 | 값 | 뜻 |
|---|---|---|
| `SCALE_MODE` | `'a'` | 화면 축척. `'a'` \| `'lambda'` |
| `SCALE_A_HALF` | `8` | `'a'` 모드의 x 반범위 (a 단위). §5 참조 — 7.5 미만 금지 |
| `RENDER_OBJECT_MAX_FRAC` | `0.20` | 물체 표식 지름 상한(패널 높이 대비) |
| `OBJECT_MARKER` | `'dot'` | `'dot'` \| `'circle'` \| `'vline'` |
| `TOTAL_SAMPLES` | `20000` | 화면 표본 수. λ/a = π 에서 파장당 11표본 |
| `ND_DISPLAY` | `14` | 장 지도·화살표 전용 격자 |
| `ND_PHYS` | `20` | 내부 진동·위상자·산란 패턴. σ/G와 같은 물리 격자 |
| `MASK_R` | `1.5` | 마스크 반경(a 단위). **축소 금지** — 1.2a면 1/R³ 발산으로 알갱이 무늬 |
| `POLARIZABILITY_FACTOR` | `0.39` | 규산염 m≈1.7의 (m²−1)/(m²+2). UI 노출 안 함 |
| `A_MIN/MAX/DEFAULT` | `0.01 / 10 / 0.1` μm | 입자 반지름 슬라이더 |
| `LAM_MIN/MAX/DEFAULT` | `0.1 / 1e6 / 0.5` μm | 파장 슬라이더 (100nm ~ 1m) |
| `LAMBDA_OVER_A_MIN` | `π` | 이 아래는 적용 범위 밖 — 흐리게 + 배지 + 회색 숫자 |
| `SCREEN_PERIOD` | `2.0` 초 | 화면 주기. λ와 무관 |
| `ARROW_N` | `40` | 축 위 화살표 개수 |

### 대표 지점 수치 (재현 확인용)

| λ/a | x | G | Q_sca | \|E_산란\|/\|E_입사\| (z=λ) |
|---|---|---|---|---|
| 3.19 | 1.9687 | 0.296 | **1.81** (빨강) | 0.46 |
| 5.01 | 1.2537 | 0.558 | 0.56 | 0.12 |
| 40.09 | 0.1567 | 0.990 | 2.4e-4 | 2.4e-4 |

`Q = 632.2·(a/λ)⁴·G`. **Q > 1은 버그가 아니라** RGD 근사(|m−1| ≪ 1)가 무너지는
신호입니다 — 값을 자르거나 계수를 조정하지 말고 빨간색 표시만 유지하세요.

---

## 8. 다음 단계 후보

우선순위는 정해져 있지 않습니다.

### a) 논문 출력 모드
원래 구상은 "행 = 물리량, 열 = λ" 전치 배치에 λ_A/λ_B 같은 색이었습니다.
**Phase 3에서 대부분의 걸림돌이 이미 치워졌습니다**: λ가 인자로 흐르고,
화면 격자가 패널 크기에서 자동 산출되며, 캔버스가 `#panelA` 같은 id로 분리되어 있습니다.
남은 것은 (1) 장 캐시를 λ별 세트로 승격, (2) 패널을 DOM에서 생성,
(3) `body[data-mode="screen"|"print"]` 최상위 모드 스위치입니다.

### b) 내부 전하의 진동 패널의 여백
세로로 긴 칸이라 정사각 지도 위아래에 빈 공간이 남습니다. 칸을 넓히거나
범례를 재배치하는 선택지가 있습니다. 기능상 문제는 없습니다.

### c) 작은 λ/a에서의 물체 표식
`'lambda'` 축척에서는 λ/a가 작을 때 물체가 여전히 커집니다(그 모드에서는
축척이 λ에 딸리므로 불가피). `'a'` 모드가 기본이라 일상적으로는 드러나지 않습니다.

---

## 9. 알아 두면 시간을 아끼는 함정들

지금까지 실제로 부딪힌 것들입니다.

- **컨트롤 줄 줄바꿈** — 판독창 글자 수가 바뀌면 캔버스 높이가 363 → 352px로 변했습니다.
  줄 수를 고정하고 판독창만 줄어들게 해서 해결. §6-8.
- **슬라이더 양자화** — 1/1000 로그 슬라이더라 한 칸이 1.6%입니다.
  `change`에서 값을 되읽으면 `input`이 정한 값이 어긋나 경계 판정이 풀립니다.
  그래서 `change`는 재계산만 합니다. λ/a = π를 정확히 맞출 수 없다는 뜻이기도 합니다
  (경계 위를 잡으려면 `Math.ceil`).
- **숨은 캔버스** — `clientWidth = 0`이면 그릴 수 없습니다. 상세 보기가 접혀 있으면
  렌더링을 건너뛰고 펼칠 때 그립니다.
- **`aspect-ratio`로 정사각형 고정** — 칸이 낮아지면 넘칩니다. 정사각형 유지는
  `blitSquare`가 `min(폭, 높이)`로 담당하게 맡기세요.
- **화면 좌표는 a 단위** — `Z_HALF = 8`은 8a라는 뜻입니다. 물리 함수를 부를 때만
  `aUm`을 곱해 μm로 바꿉니다. 오버레이(눈금 막대·경계원·표식)가 전부 a 단위라
  화면 쪽을 실단위로 바꾸면 그쪽이 전부 흔들립니다.
- **`window.RGDTest.freeze(phase)`** — 애니메이션을 멈추고 위상을 고정하는 검증용 훅입니다.
  프레임을 비교하려면 반드시 먼저 부르세요.

---

## 10. 최근 커밋

```
ddc5b3a  구조: 탭 제거 — (E)(F) 접이식 강등 + 알파벳 라벨 정리
c4f681b  설정: playwright 를 devDependencies 로 추가 + 테스트 스크립트 분리
b0c1321  화면: 축척 정책을 λ 기준에서 a 기준으로 전환
ab06aa0  기능: (A)(B)(C) 세로 3단 재배치 + 애니메이션
c8b45bb  기능: a=1 고정 해제 — a·λ 독립 슬라이더 + 실제 단위
6a54dfe  문서: 인수인계 HANDOFF.md + 2단계 프롬프트 원본 추가
56a26f8  구조: 6패널을 탭 2개로 재편 + 라벨 정리
```

작업 트리는 깨끗합니다(`verify-results.json`의 `generatedAt`만 검증 재실행 때 바뀝니다).
