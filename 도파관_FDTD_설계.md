# 도파관 = 입사파 + 산란파 중첩 시뮬레이션 (FDTD) — 설계 문서

> 작성일: 2026-06-27
> 목적: 평행판 도파관의 작동 원리를, 선원(線源)이 만드는 원통파(입사파)와
> 도체판이 만드는 산란파의 **중첩**으로 설명한다. 일반물리학 수준 학습자가
> 차단(cutoff)·소멸파·입구 반사를 직관적으로 이해하도록 한다.

---

## 1. 물리 모델 (확정)

- **2D 단면 문제.** 도선은 화면에 수직으로 놓인 무한 원통(축이 화면 밖 ẑ).
  단면에서는 점처럼 보이고, 파면은 동심원으로 퍼진다("원통형으로 사방으로").
- **편광: E_z (도선과 평행).** 전류가 도선 방향이라 방사장은 E_z 성분뿐.
  스칼라 헬름홀츠 방정식 `(∇² + k²)E_z = source` 하나로 기술된다.
- **도파관:** 위·아래 두 접지 평행 도체판, 간격 `a`. 경계조건은 판 위에서
  **E_z = 0** (디리클레 / 완전도체 접선 전기장 0).
- **장 분해(3분할의 정의):**
  - **입사파** = 평행판이 전혀 없을 때 도선이 만드는 자유공간 원통파.
  - **전체장** = 평행판이 있을 때의 실제 장.
  - **산란파** = 전체장 − 입사파.
  - 도체 위에서는 전체장 E_z = 0 이므로 산란파 = −입사파.
    → 유도전하가 입사장을 정확히 상쇄하는 물리 그림과 일치한다.

### 선원 위치 영역(regime) 정의

- 선원의 x좌표가 도파관 입구보다 **왼쪽(자유공간)**이면
  **"개구 결합(aperture coupling)"** 영역, **입구 안쪽**이면
  **"내부 여기(internal driving)"** 영역으로 구분한다.
- 두 영역 모두에서 **장 분해 정의는 동일**하다: 입사파 = 판이 없을 때 선원이
  만드는 자유공간 원통파, 전체장 = 판이 있을 때의 실제 장,
  산란파 = 전체장 − 입사파.
- 단, **"입사(incident)"라는 용어는 선원이 바깥에 있을 때만 직관적으로 맞다.**
  선원이 내부에 있을 때 패널 ①은 "판이 없었다면 이 선원이 만들었을 장
  (자유공간 기준장)"으로 라벨을 바꾼다.

### 차단 (영역별 의미 분리)

- **차단(cutoff)은 도파관 내부 모드에만 적용되는 개념이다.**
  이 편광에는 TEM 모드가 없고 최저 모드가 n=1이라 **차단파장 λ_c = 2a**.
  `λ ≥ 2a` (즉 `k ≤ kc`)이면 **도파관 내부의 모든 모드가 소멸파(evanescent)**가
  되며(임계 `λ = 2a`는 `κ = 0`), 소멸 감쇠상수 `κ = sqrt((π/a)² − k²)`, `k = 2π/λ`.
- **내부 여기 영역:** 선원이 도파관 안에 있으면, `λ>2a`일 때 선원 양쪽(±x)으로
  소멸파만 생기고 진행파가 없다. **이것이 차단의 가장 순수한 시연**이다.
- **개구 결합 영역:** 선원이 바깥에 있으면, 입구 왼쪽 자유공간에는 항상 전파파가
  존재한다(소멸은 입구 안으로 들어간 성분에만 해당).

### 기하 배치 (확정)

- 도파관(가로로 누운 위·아래 판)은 화면 **오른쪽 약 3/4**를 차지하고,
  **입구(열린 끝)는 왼쪽**을 향한다.
- 도선(점 파원)은 왼쪽 자유공간에서 시작해, 마우스로 **입구를 통과시켜
  도파관 안으로 넣거나 다시 왼쪽으로 빼낼** 수 있다.
- 진행 방향(전기장 세기 그래프의 x축)은 **가로**.

---

## 2. 계산 엔진 — FDTD (시간영역)

선택 근거: 입구(열린 끝)에서의 반사·회절과 도선을 입구로 넣고 빼는 동작까지
모두 자연스럽게 담으려면 시간영역 격자 해법이 가장 적합하고, 파동이 실제로
움직이는 모습이 수업에서 가장 직관적이다. 더블클릭 실행 가능한 vanilla JS로
구현 가능.

- **2D TM_z leapfrog** 업데이트(필드 `E_z`, `H_x`, `H_y`), `Float32Array` 격자.
- **두 격자를 동시에 실행한다.**
  - ① 자유공간 격자(판 없음) = **입사파 / 자유공간 기준장**
  - ② 판 있는 격자 = **전체장**
  - **산란파 = ② − ①** 를 매 프레임 계산 (별도 시뮬레이션 불필요).
- **파원:** 부드럽게 켜지는 연속 사인 선원 `J_z = ramp(t)·sin(ωt)`
  (시작 과도 충격을 줄이기 위한 램프 포함).
- **선원 양자화 일관성:** 선원이 도체판에 매우 가까워질 수 있으므로(내부 여기
  영역), 선원 격자점이 판 격자점과 겹치지 않도록 **최소 1~2셀 간격으로
  클램프**한다. 또한 두 격자(①②)에서 선원의 **양자화된 위치를 항상 동일하게**
  처리해, 산란파(②−①)에 인공물(artifact)이 생기지 않게 한다.
- **도체판:** 판에 해당하는 격자점에서 매 스텝 `E_z = 0` 강제.
- **흡수경계:** **분리장(Berenger) PML**(두께 ~10셀). 두 격자에 동일 PML을
  적용해 산란 패널(②)의 경계 인공물을 최소화한다. (Mur 1차로는 ②에 인공물이
  섞여 핵심 교육 목표가 깨지므로 PML을 범위 안으로 둔다.)
- **정상상태 진폭 추출:** 구동주파수에서 셀별 위상자(phasor)를 1주기 동안
  러닝 DFT(Goertzel류)로 누적 → 깜빡임 없는 `|E_z|` 지도와 그래프.
- **CFL 안정조건** 충족(2D: `c·dt ≤ dx/√2`).
- **성능:** 480×240 격자 × 2개, 프레임당 수 스텝 → Float32 typed array로
  실시간(60fps 목표) 가능.

---

## 3. 화면 / UI

### 레이아웃 (세로 3단 + 하단 그래프 + 우측 조절판)
```
┌────────────────────┐ ┌──────┐
│ ① 입사파 (원통파)   │ │ 조   │
├────────────────────┤ │ 절   │
│ ② 산란파 (도파관)   │ │ 판   │
├────────────────────┤ │      │
│ ③ 중첩 (전체장)     │ │ λ    │
├────────────────────┤ │ a    │
│ |E| vs x 그래프      │ │ …    │
└────────────────────┘ └──────┘
  x축 = 진행방향 (세 패널과 세로 정렬)
```
- 패널 ①②③: 순간장 `Re[E_z]`를 **적·청 발산 컬러맵**으로(기존 자료와 동일 톤).
  도파관 판·도선·입구를 세 패널에서 공통 정렬.
- 하단 그래프: 도파관 중심선(y = a/2)을 따라 **|E_z| vs x** 포락선.
  x축이 위 패널과 정렬되어 "이 지점의 지수감소"를 바로 짚을 수 있음.

### 패널 라벨 (동적 전환)
- **패널 ①:** 개구 결합 영역에서는 **"① 입사파(원통파)"**,
  내부 여기 영역에서는 **"① 자유공간 기준장(판 없을 때)"**로 라벨을 바꾼다.
- **패널 ②:** **"② 산란파(경계조건이 강제)"**로 고정.
- **패널 ③:** "③ 중첩(전체장)".

### 선원 위치 영역 배지 (신규)
- 화면에 현재 영역을 표시한다: **"선원: 자유공간(개구 결합)"** 또는
  **"선원: 도파관 내부(내부 여기)"**.
- 선원 x좌표가 입구 경계를 넘는 순간 자동 전환.

### 마우스
- **도선을 2D로 자유 드래그**: 좌우 = 입구 in/out, 상하 = 판 사이 위치.

### 모드 여기와 선원 y위치 (캡션 안내)
- n=1 모드는 **y=a/2(중심선)에서 배(antinode)**, **y=0·y=a(판)에서 마디(node)**.
  - 선원을 **중심선**에 두면 n=1 모드를 **최대로 여기**.
  - 선원을 **판 가까이** 두면 n=1 여기가 약해지고 더 높은 모드가 섞인다.
- 상하 드래그로 이 현상을 관찰할 수 있다. 단, 학습자가 "중심선에서는 잘 되고
  판 근처에서는 약해지는" 현상을 **차단과 혼동하지 않도록**, 선원 y위치에 따른
  모드 여기 세기를 **별도 캡션으로 안내**한다.

### 조절판 컨트롤
- 파장 λ 슬라이더 (차단 경계를 넘나들 수 있는 범위)
- 도파관 간격 a 슬라이더
- 진폭
- 재생 / 일시정지 · 속도
- 리셋
- (선택) 경계·격자 표시 토글

---

## 4. 측정 · 표시 (정성 + 간단 정량)

### 차단 배지 (영역 분기)
- **공통:** "도파관 내부 — λ vs λ_c=2a 비교 → 내부 전파 모드 수 표시".
  배지 문구는 **항상 내부 한정**으로 적는다
  (예: "도파관 내부: 전파 모드 없음(소멸)").
- **내부 여기 영역, λ>2a:** "선원 양쪽으로 소멸파만(진행파 없음)".
  그래프는 선원에서 양방향으로 **지수감소하는 대칭 포락선**.
- **내부 여기 영역, λ<2a:** "선원 양쪽으로 진행파".
  그래프는 양방향 진행파(+판 끝 반사가 있으면 정재파 리플).
- **개구 결합 영역:** 아래 입구 결합/반사 표시를 유지하되, 배지는
  **"도파관 내부" 한정**으로 명시한다.

### 입구 반사 (개구 결합 영역)
- 전파 영역: 그래프의 도파관 내부 정재파 리플에서 정재파비 SWR을 읽어
  `|R| = (SWR − 1)/(SWR + 1)` → "반사율 %"로 표시.
- 차단 영역: 전파 모드가 없으므로 "거의 전반사(≈100%)"로 표기.

### 감쇠상수 검증 (내부 여기, λ>2a 한정)
- 선원이 안에 있는 경우 κ를 깨끗하게 측정할 수 있다. 선원에서 멀어지는 방향의
  중심선 |E_z| 포락선에 **지수함수 fit**을 적용해, 측정된 감쇠상수를
  이론값 `κ = √((π/a)² − (2π/λ)²)`와 **나란히 표시**한다.
- 학습자가 "지수감소가 정말 이론과 맞는가"를 눈으로 확인할 수 있다.

### 표시 예시
```
[내부 여기 / λ > 2a]
선원: 도파관 내부(내부 여기)
λ = 30 cm,  λ_c = 2a = 24 cm
도파관 내부: 전파 모드 없음(소멸) — 선원 양쪽으로 소멸파만
감쇠상수 κ: 측정 0.49 /cm  vs  이론 0.51 /cm
그래프: 선원에서 양방향 지수감소 대칭 포락선
```

---

## 5. 단위 · 기본값 (조정 가능)

- λ, a 는 cm 표시. 기본값 **a = 12 cm → λ_c = 24 cm**.
- λ 슬라이더 **5–60 cm** (차단 경계 24 cm를 쉽게 넘나듦).
- f ≈ c/λ (GHz) 부가 표시.
- 격자는 내부적으로 셀 단위(예 480×240) 고정, `dx`로 cm 환산.

---

## 6. 파일 구조

- `index.html` / `style.css` / `script.js` 3파일.
- 클래식 `<script src>` · `<link>` 연결 (ES 모듈·로컬 fetch 사용 안 함)
  → **더블클릭으로 바로 실행.** (CLAUDE.md 기본값)

---

## 7. 범위 밖 (YAGNI) — 원하면 나중에 확장

- 모달 분해 풀 정량 (|R|∠위상, 모드별 전력, 투과율 |T|²).
- 영상파원 / 광선 오버레이 (기존 `waveguide_image_source.html`와 개념 연결).
- 색맵·스케일 고급 옵션, CPML 등 더 고급 흡수경계(기본 분리장 PML로 충분).

---

## 합의된 기본 결정 사항

| 항목 | 결정 |
|------|------|
| 계산 엔진 | FDTD (시간영역) |
| 화면 구성 | 세로 3단(입사/산란/중첩) + 하단 |E| 그래프 |
| 선원 영역 | 개구 결합(바깥) / 내부 여기(안쪽) 구분, 배지·라벨 동적 전환 |
| 분석 정밀도 | 정성 + 간단 정량 (반사율 %, 차단 배지, κ 검증) |
| 도선 드래그 | 2D 자유 (좌우 in/out + 상하), 판과 최소 간격 클램프 |
| 기본 a | 12 cm (λ_c = 24 cm), λ 5–60 cm |
| 파일 | index/style/script 3파일, 더블클릭 실행 |
