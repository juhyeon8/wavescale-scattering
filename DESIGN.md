# RGD 소입자 산란 물리 코어 — 설계 (1단계: 물리 코어 + 검증)

- 날짜: 2026-07-25
- 범위: `physics.js` + `verify.js` (+ `DESIGN.md`, `README.md`). **UI는 이 단계에서 만들지 않는다.**
- 원본 지시: `RGD_1단계_프롬프트.md` (커밋되어 있는 확정 스펙) + 브레인스토밍 중 확정된 세부 결정
- 목적: 파장이 입자 크기보다 훨씬 긴 영역(레일리)과 파장이 입자 크기와 비슷한 영역(회절) 사이의 경계를, RGD(Rayleigh–Gans–Debye) 근사로 수치적으로 정하는 논문용 시뮬레이션의 물리 코어. 통합 변수는 λ/a.

## 1. 물리 모형 (변경 불가 — RGD_1단계_프롬프트.md 확정 사항)

- 입사파 +z 진행, 전기장 x축 편광, 시간 규약 `e^{-iωt}`
- 산란체: 반지름 `a`인 구, **a = 1 고정**. **λ가 조작 변인**이며 a=1이므로 λ 값이 곧 λ/a
- `k = 2π/λ`, 크기 매개변수 `x = ka = 2π/λ`
- `E₀ = 1`
- **총 분극률 `α_total = 1` 고정. 개별 쌍극자 `α = α_total/N = 1/N`** (개별 α를 상수로 고정하면 N이 λ에 따라 바뀌면서 총 분극률이 격자 해상도에 오염되므로 반드시 α_total을 고정하고 나눔)
- 쌍극자: `p_j = α·E₀·x̂·exp(i k z_j)`
- 원거리 산란파: `E_sca(n̂) ∝ (k²αE₀/r)·sinΘ·F(q)`
  - `Θ`: n̂와 x̂ 사이 각, 편광 인자 `sin²Θ = 1 − (n̂·x̂)²`
  - `F(q) = Σ_j exp(i q·r_j)`, `q = k(ẑ − n̂)`, `|q| = 2k·sin(θ/2)`
  - `dσ/dΩ ∝ sin²Θ·|F(q)|²`

### φ 적분의 해석적 처리 (1D 경로 전용)

구형 산란체에서 `|F|`는 `|q|`(즉 θ)에만 의존한다는 가정 하에:

```
∫₀^{2π} (1 − sin²θ·cos²φ) dφ = π(1 + cos²θ)

σ = (8π/3)·k⁴·α_total²·G(x)
G(x) = (3/8)·∫_{−1}^{1} (1 + μ²)·|F(μ)/N|² dμ,   μ = cosθ
```

- `G(x)`가 이 시뮬레이션의 핵심 단일 지표. `G(0) = 1`이며 1에서의 이탈량이 레일리 근사의 붕괴 정도.
- 1D 경로에서 `F(μ)`는 **실제 격자 합을 φ=0 슬라이스로 고정**해 계산한다 (등방성 가정). 이 가정이 실제로 성립하는지는 §4의 2D 대조로 검증한다 — φ=0은 정육면체 격자의 대칭면이라 이방성 검출에 가장 불리한 방향이므로, 검증에서는 φ=0과 φ=45°를 별도로 비교한다(§4의 검증 5b).
- μ에 대해 Gauss–Legendre. 노드/가중치는 **Newton법으로 순수 JS 직접 구현** (외부 라이브러리 금지). 기본 32점, 64점 배증 시 상대 변화를 verify에서 출력. 피적분 함수는 μ의 해석 함수이므로 지수적으로 수렴해야 하며, 느리면 버그로 간주한다.

### 편광 인자 이중 계산 금지 (반드시 각 함수 상단 주석에 명시)

- `sigma1D`: `(1 + μ²)`를 쓴다 — 이미 `∫ sin²Θ dφ`가 적분된 결과이므로 별도로 `sin²Θ`를 곱하면 안 된다.
- `sigma2D` / `dSigmaDOmega`: `sin²Θ = 1 − (n̂·x̂)²`를 명시적으로 쓴다.

## 2. 이산화

- 배치: 반지름 a 안에 들어오는 격자점만 취하는 정육면체 격자. **난수 배치 금지** (|F|²에 샷 노이즈 유입)
- **h가 아니라 n_d(지름당 격자점 수)를 정수로 지정**한다. `h = 2a/n_d`, N은 종속 변수
- 제약: `k·h ≤ 0.3 ⟺ n_d ≥ 20x/3`, 기하학적 충실도를 위해 `n_d ≥ 20`
  → `chooseNd(x) = max(20, ceil(20x/3))` 자동 적용
- x ≤ 2 범위에서는 `n_d = 20` (N ≈ 4200)이면 충분
- **격자점 배치는 셀 중심(cell-centered)으로 고정한다**: `x_i = −a + (i + 0.5)·h,  i = 0 … n_d−1` (y, z 동일). n_d의 홀짝과 무관하게 항상 원점 대칭이 보장되며, 검증 6(`Im(F)/N = 0`)의 전제가 구현자의 임의 선택에 좌우되지 않는다. 이 근거를 코드 주석에 남길 것

## 3. physics.js — 순수 계산 API (DOM/Node API 금지, console/fs/process 금지)

CommonJS. IIFE로 감싼 `RGD` 단일 네임스페이스만 전역에 노출. 파일 맨 아래 조건부 export:

```javascript
if (typeof module !== 'undefined' && module.exports) module.exports = RGD;
```

이렇게 Node에서는 `require`로, 브라우저에서는 `<script src>`로 동일 파일을 그대로 재사용한다 (2단계 UI 대비). `package.json`은 만들지 않는다.

| 함수 | 역할 |
|---|---|
| `RGD.chooseNd(x)` | `max(20, ceil(20x/3))` |
| `RGD.buildDipoleGrid(a, nd)` | 정육면체 격자를 반지름 a 구 안으로 잘라 `{points, N, h, columns}` 반환 (a=1 고정이지만 재사용성을 위해 인자로 받음). `columns`은 `sigma1D`의 φ=0 열 축약 최적화에 쓰는 `[{x, z, m}]` 목록 — §5 참고 |
| `RGD.qVector(k, theta, phi)` | `q = k(ẑ − n̂)` |
| `RGD.formFactor(points, qVec)` | `F(q) = Σ exp(i q·r_j)` → `{re, im}` |
| `RGD.analyticShapeFactor(u)` | `f(u) = 3(sin u − u·cos u)/u³`, u→0 급수 전개로 0/0 회피 |
| `RGD.gaussLegendreNodes(n)` | Newton법으로 `[-1,1]` 노드·가중치 계산, **n별로 결과 캐싱** (2단계 슬라이더 대비) |
| `RGD.dSigmaDOmega(points, k, theta, phi)` | `sin²Θ·|F(q)|²` — 명시적 편광 인자 사용. 극좌표 산란 패턴(2단계 UI: 편광면 φ=0, 수직면 φ=90°)의 기반 함수 |
| `RGD.sigma1D(grid, k, alphaTotal, {n=32})` | `(1+μ²)` 해석적 φ-적분 경로. `{sigma, G}` 반환. **자기 방어**: `grid.h`로 `k·h`를 계산해 `0.3` 초과 시 경고 출력 |
| `RGD.sigma2D(grid, k, alphaTotal, {nTheta=32, nPhi=16})` | `dSigmaDOmega` 위에 얹은 완전한 2D 적분 (θ: Gauss–Legendre, φ: 16점 균일 격자). **자기 방어**: sigma1D와 동일하게 `grid.h`로 `k·h ≤ 0.3` 확인·경고 |

`sigma1D`와 `sigma2D`는 **시그니처를 통일**한다 — 둘 다 원시 `points` 배열이 아니라 `buildDipoleGrid`가 반환한 `{points, N, h, columns}` 객체(`grid`)를 받고, 둘 다 동일한 `k·h ≤ 0.3` 자기 방어를 갖는다. 형제 함수의 시그니처가 갈리면 2단계에서 잘못 호출될 위험이 있고, 통일 비용은 0이다. 단 `dSigmaDOmega`는 예외적으로 원시 `points` 배열을 받는다 — 각도마다 호출되는 저수준 함수이므로 `k·h` 자기 방어를 넣으면 경고가 폭주하기 때문이며, 이는 의도된 설계다.

> **sigma2D는 physics.js에 둔다** (verify.js가 아님) — 이유: (1) verify.js에만 있으면 1D를 검증하는 코드 자체가 검증받지 않은 별도 구현이 된다. (2) verify.js는 fs/process를 쓰므로 브라우저에서 재사용 불가. (3) 2단계 UI의 극좌표 산란 패턴 그래프가 `dSigmaDOmega`를 그대로 필요로 한다.
>
> **nPhi 기본값이 16인 이유**: "φ 8점이면 기계 정밀도"라는 근거는 `|F|`가 φ에 무관(등방)할 때만 성립한다. sigma2D는 그 등방성 가정을 검증하는 경로이므로 이 가정을 쓸 수 없다 — 정육면체 격자는 z축 둘레 4회 대칭이라 `|F|²`에 `cos4φ`, `cos8φ` 성분이 있고, 8점 균일 격자는 `cos8φ`를 DC(상수)로 앨리어싱시켜 검증 5a에 상수 편향을 만든다. 16점이면 `cos8φ`까지 정확히 적분된다.

## 4. verify.js — 검증 전용 (console/fs/process는 여기서만 사용)

해석해 `f(u) = 3(sin u − u·cos u)/u³` (u = qa = 2x·sin(θ/2))와 대조.

각 항목은 `console.table`로 `[측정값, 임계값, PASS/FAIL]`을 출력한다.

| # | 항목 | 방법 | 임계값 |
|---|---|---|---|
| 1 | 형상 인자 수렴 | x = 0.1, 0.5, 1.0, 2.0 각각 θ=0°~180°를 **1° 간격 181점**으로 샘플링해 `\|F/N − f(u)\|` **절대 오차**의 최댓값 (상대 오차 아님 — 이유는 표 아래 참고) | 0.01 (절대) |
| 2 | 전방 산란 불변 (입사파 위상 누락 검출) | θ=0 (q=0)에서 x와 무관하게 `F/N = 1`. `F = Σ exp(i k z_j)·exp(−i k n̂·r_j)`에서 입사파 위상 `exp(i k z_j)`를 누락하면 `q = −k n̂`가 되어 θ=0에서 `q = −kẑ ≠ 0`이 된다 — 즉 이 검증은 q의 전역 부호(관측 불가능)가 아니라 입사파 위상 인자의 누락을 잡는 테스트다 | 1e-12 |
| 3 | 레일리 극한 — 기울기 | `x < 0.05` (λ/a > 125) 구간의 점들만으로 최소제곱: `log σ` vs `log(1/λ)` 기울기 → 4.00 수렴 | \|기울기−4\| < 0.01 |
| 3′ | 레일리 극한 — 계수 | **21cm 프리셋(x = 3.0e-6)에서 평가** — 별도 계산 불필요, sweep의 해당 행 재사용. `G(x) → 1.000` (기울기보다 민감: 편광 인자·입체각 요소·규격화 중 하나라도 어긋나면 여기서 드러남) | 1e-3 |
| 4 | N 비의존성 | **x = 2.0에서 평가** (최악 조건). α_total 고정이므로 σ는 N에 무관해야 함. `n_d = 20, 28, 40, 56`에서 σ 상대 편차 (n_d를 정확히 배수로 맞출 필요 없음 — 구로 자르면 N이 계단식으로 뜀) | 0.5% |
| 5a | 격자 이방성 (2D 대조) | **x = 2.0에서 평가** (최악 조건). `\|σ_2D − σ_1D\| / σ_1D`. 초과 시 n_d를 늘리라는 경고 | 0.5% |
| 5b | 격자 이방성 (직접 진단) | **x = 2.0에서 평가** (최악 조건). `θ ∈ {60°, 90°, 120°}` 각각에서 지표 `\|\|F(θ,φ=0)\| − \|F(θ,φ=45°)\|\| / \|F(θ,φ=0)\|` (진폭의 상대 편차)를 계산해 그중 최댓값을 사용. θ=0°/180°는 q 방향이 φ와 무관해져(θ=180°에서 q=2kẑ) 진단이 축퇴되므로 제외. 구라면 동일해야 하며 차이가 곧 격자 이방성. φ=0은 정육면체 대칭면이라 이방성 검출에 가장 불리한 방향이므로 필요 | 0.5% |
| 6 | 격자 중심 확인 | 격자가 원점 대칭이면 `Im(F)/N`은 항상 0이어야 함 (σ에는 영향 없지만 2단계 위상자 다이어그램이 회전해 보이는 원인이 됨) | 1e-12 |

**4, 5a, 5b가 모두 x=2.0에서 평가되는 이유**: 셋 다 격자 이산화/자유도 관련 오차를 재는 항목이므로, 오차가 가장 크게 나타나는 최악 조건(x=2.0, 탐색 범위 상한)에서 판정한다는 원칙을 따른다.

**검증 1이 상대 오차가 아니라 절대 오차인 이유**: `f`는 `f(0)=1`로 규격화되어 있어 절대 오차가 자연스럽다. 상대 오차로 두면 `x=2, θ=180°`에서 `u=4, f(4)≈0.087`이라 상대 1%가 절대 `8.7e-4`를 요구해 `n_d=20`에서 과도한 기준이 되고, `f`의 첫 영점이 `u=4.493 (x≈2.246)`이라 탐색 범위를 조금만 넓혀도 상대 오차 자체가 정의되지 않는다.

추가로 sweep 전 구간에서 `chooseNd(x)`가 실제로 상수(20)로 유지되었는지 명시적으로 확인·출력한다 — `x ≤ 3`에서는 항상 20을 반환하므로 현재 범위에서는 상수이지만, 범위를 넓혔을 때 N이 뛰어 곡선에 계단이 생기는 것을 사전에 방지하기 위한 회귀 성격의 체크다.

마지막에 전체 PASS/FAIL 요약 한 줄.

### 실행 순서 (계산 순서 ≠ 파일 기록 조건)

검증 3(기울기)은 `x<0.05` 구간의 여러 (λ, σ) 점을, 검증 3′(계수)은 21cm 프리셋 행을 필요로 하므로, sweep 계산이 검증 8항목보다 **먼저** 이루어져야 한다. "검증 통과 후"라는 표현은 계산 순서가 아니라 **파일 기록 조건**을 뜻한다. 실행 순서를 다음으로 고정한다:

1. sweep 계산 (60점 + 프리셋 2행) — **메모리에만** 보관, 아직 파일에 쓰지 않는다
2. 검증 8항목 실행 (검증 3, 3′은 1에서 계산해 둔 sweep 데이터를 그대로 사용)
3. 전부 PASS일 때만 `verify-results.json`/`sweep.csv`를 기록한다. 하나라도 FAIL이면 파일을 쓰지 않고 `process.exit(1)`

### 출력 파일 (모두 git에 커밋 — gitignore 금지, 논문 표 원본 데이터)

- `verify-results.json` — 파라미터 헤더 포함 (재현성 확보)
- `sweep.csv` — 열: `lambda, x, nd, N, sigma, G` (이 6열 고정). 기본 범위 `λ/a ∈ [π, 1e3]` 로그 간격, **60점**(`x = 2π/(λ/a) ∈ [0.00628, 2]`), 그리고 다음 2개 프리셋을 별도 단일 행으로 추가 (성간 티끌 a=0.1μm 기준, sweep 로그 격자에 정확히 걸리지 않으므로 별도 행 필요):
  - 21cm 전파: `λ/a = 2.1e6, x = 3.0e-6`
  - 가시광선 500nm: `λ/a = 5, x = 1.2566` — 논문의 "가시광선은 이미 레일리를 벗어나 있다" 논증에 이 G 값을 사용
  - 시작점을 `λ/a = 3.14`가 아니라 **`λ/a = π` (x = 2.0000 정확히)**로 둔다 — 3.14는 `x = 2.00064`가 되어 §6의 "x ≤ 2 제한"을 미세하게 위반하기 때문

## 5. 구현 순서

1. 1D/2D 모두 소박한 N-항 합(naive loop)으로 먼저 구현
2. verify.js 검증 8항목(1, 2, 3, 3′, 4, 5a, 5b, 6) 전부 PASS 시킨다 — **PASS 전에는 다음 단계로 넘어가지 않는다**
3. 성능 최적화: **`sigma1D` 전용**으로 φ=0 열(column) 축약을 적용한다 (근사가 아니라 정확한 항등식).
   φ=0 슬라이스에서는 `q = k(−sinθ, 0, 1−cosθ)`이므로 항상 `q_y = 0` — 즉 `q·r_j`에 `y_j`가 들어가지 않는다. 따라서 격자점을 `(x, z)` 열로 묶고 그 열에 속한 y 격자점 개수를 무게 `m(x_i, z_k)`로 쓰면:

   ```
   F = Σ_columns m(x_i, z_k) · exp(i(q_x·x_i + q_z·z_k))
   ```

   `n_d=20`에서 항 수가 약 4200 → 314(≈ `π·n_d²/4`)로 13배 줄어든다. `buildDipoleGrid`가 반환하는 `columns: [{x, z, m}]`을 사용한다.
   **`sigma2D`는 φ≠0을 다루므로 이 최적화를 쓸 수 없다 — 소박한 N-항 합 그대로 둔다.**
   (z 층별 히스토그램 방식은 쓰지 않는다 — 이는 `q ∥ ẑ`를 가정하는데, φ=0 슬라이스에서 q 방향은 θ마다 바뀌므로 이 설계에는 적용되지 않는다.)
   최적화 전후 `sigma1D` 결과가 일치하는지 반드시 대조한다.

### 첫 실행 판정 지침 — 실패를 오진하지 않기 위한 분류

임계값 중 일부는 추정치이며, 첫 실행이 곧 실측이다. 어떤 실패가 버그이고 어떤 실패가 이산화(격자 해상도) 탓인지 구분하기 위해 아래 분류를 따르고, verify.js 출력에도 반영한다.

**(a) 기계 정밀도여야 하는 항목 — 실패 시 확실한 버그, `n_d`를 만지지 말고 코드를 볼 것**

- 검증 2: θ=0에서 q=0이 정확히 성립하므로 모든 항이 정확히 1. `F/N = 1`이 비트 단위로 나와야 한다.
- 검증 6: 셀 중심 배치로 ±쌍이 정확히 상쇄. `Im(F)/N` ~ 1e-16 수준이어야 한다.
- 검증 3′: `x=3e-6`에서 물리적 이탈량은 `G−1 ≈ −0.4x² ≈ −3.6e-12`. 임계값 `1e-3`은 매우 헐거우므로, **`G−1`을 지수 표기로 전체 자릿수 출력**한다 — `1e-6` 수준의 계통 편차는 임계값을 통과하더라도 규격화 오류의 신호이므로 값 자체가 보여야 한다.

**(b) 이산화가 원인일 수 있는 항목 — `n_d` 의존성부터 확인**

- 검증 1의 x=2.0 지점 (x=0.1/0.5/1.0은 통과하는데 2.0만 실패하면 이산화 신호)
- 검증 4, 5a, 5b

진단 절차: 실패한 항목을 `n_d = 20, 28, 40, 56`에서 재계산해 표로 출력한다. 오차가 `n_d`에 따라 단조 감소하면 이산화, 오차가 `n_d`와 무관하게 일정하면 버그다. **verify.js는 (b) 항목이 실패하면 이 `n_d` 스캔을 자동으로 수행하고 표로 출력한다.**

**(c) 검증 5b가 가장 빠듯할 것으로 예상됨**

`x=2, θ=90°`에서 `|F|/N ≈ 0.4`이고, `n_d=20`의 계단 경계 오차가 절대 `0.008` 수준이면 상대 `2%`가 되어 임계값 `0.5%`를 넘길 수 있다. 이 경우 임계값을 늘리거나 버그를 찾지 말고, **검증용 `n_d`를 올려 `0.5%`를 만족하는 최소 `n_d`를 찾아 보고**한다 (상용 sweep은 `n_d=20`을 그대로 유지 — 검증용 `n_d`와 상용 `n_d`는 별개). 이 결과를 README.md에 "상용 sweep(`n_d=20`)의 x=2 지점 정확도는 약 N%"로 명시해, 논문에서 정량적 한계를 정직하게 기술할 수 있게 한다.

## 6. 모형의 한계 (physics.js 주석 + README.md에 명시)

- **Born 근사**: 쌍극자 간 상호작용 무시
- **RGD 적용 조건**: `|m−1| ≪ 1`이고 `2x|m−1| ≪ 1`. 실제 규산염 성간 티끌은 m≈1.7이므로 정량적으로는 맞지 않음 — 목적은 정량적 예측이 아니라 경향과 기제 확인
- 그림자 효과·흡수 없음 → x ≳ 3에서는 물리적으로 신뢰 불가. **x ≤ 2로 제한**
- 기하학적 축(2πa/λ)만 다룸. 분광학적 축(ω/ω₀)은 다루지 않음
- α를 x에 무관한 상수로 두는 근거: ω ≪ ω₀ 영역에서는 복원력이 지배해 변위 진폭과 분극률이 거의 일정하다는 물리적 가정 (코드 주석에 명시)

## 7. 산출물 및 파일 위치

루트 평면 구조 (이전 도파관 프로젝트와 동일):

| 파일 | 내용 |
|---|---|
| `physics.js` | 순수 계산 함수 (RGD 네임스페이스) |
| `verify.js` | 검증 8항목(1, 2, 3, 3′, 4, 5a, 5b, 6) + λ 훑기 실행 |
| `DESIGN.md` | 이 설계 문서(`2026-07-25-rgd-scattering-design.md`) 자체를 그대로 커밋한 것. 별도 요약본을 두지 않는다 — 요약본은 원본과 갈라져 어느 쪽이 최신인지 모호해지므로 |
| `README.md` | 모형·한계·검증 결과 요약 |
| `verify-results.json`, `sweep.csv` | 검증/훑기 결과 (커밋 대상) |

## 8. 범위 밖 (2단계 이후)

- UI, 극좌표 산란 패턴 렌더링, 위상자 다이어그램, λ 슬라이더 — `dSigmaDOmega`/`gaussLegendreNodes` 캐싱 등은 이번 단계에서 **재사용을 염두에 두고만** 설계하고, 실제 UI 구현은 하지 않는다.
