# 2단계 프롬프트 — 산란 시각화 UI (브레인스토밍 재개용)

> 이 파일은 2단계 UI 브레인스토밍을 이어서 시작할 때 그대로 붙여넣어 쓴다.
> 아직 설계가 확정되지 않았다 — "결정할 것" 섹션부터 정리하고 시작한다.

## 1단계 현황 (완료, 안정)

- `physics.js`: RGD 점쌍극자 산란 물리 코어. IIFE로 감싼 `RGD` 네임스페이스,
  조건부 export(`module.exports`), DOM/Node 전용 API 없음 — Node `require`와
  브라우저 `<script src>` 양쪽에서 그대로 재사용 가능
- `verify.js`: 검증 8항목(1, 2, 3, 3′, 4, 5a, 5b, 6) + n_d 상수성 회귀 체크,
  총 9개 전부 PASS
- API 표면: `chooseNd(x)`, `buildDipoleGrid(a, nd)`, `qVector(k, theta, phi)`,
  `formFactor(points, qVec)`, `formFactorPhi0Columns(columns, qx, qz)`,
  `analyticShapeFactor(u)`, `gaussLegendreNodes(n)`,
  `dSigmaDOmega(points, k, theta, phi)`, `sigma1D(grid, k, alphaTotal, opts)`,
  `sigma2D(grid, k, alphaTotal, opts)`
- 무차원화: **a = 1 고정, λ가 조작 변인**. 조작 변수는 `x = ka = 2π/λ` **하나뿐**.
  `y₀/a`나 도선 간격 같은 개념은 이 물리 모형에 없다
- RGD는 Born 근사이므로 "전체장 = 입사파 + 산란파" 같은 개념도 없다 —
  다루는 것은 산란파(`F(q)`, `dSigmaDOmega`, `sigma1D`/`sigma2D`)뿐

## 반드시 먼저 바로잡을 것

직전 세션 막바지에 나온 두 번째 3패널 프롬프트 초안에 "기존 도파관
시뮬레이션(도체 도선 배열 벽, MoM 산란파 계산, 전체장=입사파+산란파,
λ/a·y₀/a·도선간격 d 슬라이더)"이라는 문구가 들어 있었다. 이는 이번 세션
초반에 저장소에서 완전히 삭제한 **옛 도파관 FDTD 프로젝트**
(`feature/waveguide-fdtd` 브랜치, `fdtd.js` 등, 커밋 `0fedd25`로 삭제 완료)를
가리키는 문구이며, 지금의 RGD 물리 코어와는 무관하다. 다음 세션에서는 이
문구를 걷어내고, RGD 물리 코어의 실제 API(`dSigmaDOmega`, 개별 쌍극자 위상
`exp(ikz_j)`)를 기준으로 다시 쓴다.

## 결정할 것 — 세션 중 두 안이 서로 다르게 나왔고, 아직 확인받지 못했다

### 안 A — 4화면 + λ_A/λ_B 비교 모드 (먼저 나온 안)

- 화면1 내부 위상 지도 · 화면2 위상자 다이어그램 · 화면3 극좌표 산란 패턴 ·
  화면4 G(x) 곡선(내보내기 없음, "지금 λ가 곡선 어디에 있는지" 보여주는
  탐색용 마커만 — 정량 곡선 자체는 `sweep.csv`에서 별도 작성)
- 화면1~3은 λ_A/λ_B **2열 비교 모드**를 지원. 같은 스케일로 두 열을
  동시에 렌더링하고 그 상태 그대로 SVG로 내보낸다(스냅샷 두 번 찍어
  나중에 붙이면 축 범위·색 스케일·캔버스 크기가 인쇄에서 어긋나 보임).
  흐름: 슬라이더 탐색 → λ_A/λ_B 확정 → 비교 모드 → 내보내기
- 프리셋 버튼: 21cm 전파(λ/a=2.1e6) · 가시광선 500nm(λ/a=5) ·
  경계 영역(λ/a=10, 20, 100)
- 고정 스케일 토글: 축 범위를 λ와 무관하게 고정. 비교 모드에서는 기본 ON
- 모든 그래프에 축 이름·눈금값 명시, 진폭인지 세기인지 축 이름에 명확히 표기

### 안 B — 3패널, 슬라이더 하나로 실시간 연동 (나중에 "다시 쓸게"로 시작한 안)

- 구성: 극좌표 산란 패턴 패널 + 위상자 다이어그램 패널(상단, 좌우로 나란히,
  정사각형에 가깝게 크게) + λ 슬라이더·측정값(하단, 얇은 컨트롤 스트립)
- 표준 노트북 화면(16:9)에서 **스크롤 없이** 세 요소가 다 들어와야 함.
  높이는 vh 기준 반응형으로, 라벨이 안 읽힐 만큼 작아지는 건 피함
- λ_A/λ_B **동시 비교 안 함** — "슬라이더로 옮겨가며 관찰"이 기본 흐름.
  탭/단계 분리 구조도 쓰지 않음(세 요소는 항상 동시에 보임)
- (선택, 가치 판단 후 결정) freeze/ghost 토글: 현재 곡선을 옅은 참조선으로
  고정해두고 슬라이더를 옮기면 실시간 곡선이 그 위에 겹쳐져 순간적인 A/B
  비교가 가능. 패널 수를 늘리지 않는 선에서만
- 내보내기·프리셋·고정 스케일 토글 언급이 없음 — 폐기된 것인지, 축소된
  형태로(예: freeze/ghost에 내보내기 포함) 유지되는지 미정

### 다음 세션 시작하자마자 확인할 질문

1. 안 B가 안 A를 **완전히 대체**하는가? 아니면 안 B가 "기본 화면"이고
   안 A의 비교/내보내기/프리셋 기능은 별도 모드로 남는가?
2. (안 B가 기본이라면) SVG 내보내기를 이번 단계에서 아예 뺄지, freeze/ghost
   상태에 내보내기를 붙일지
3. 위상자 다이어그램의 실제 표현 방식: 위상 순서로 정렬한 누적합 경로
   ("phasor walk", N이 커도 하나의 경로선만 그려 안 뭉개짐) vs 개별
   쌍극자 화살표를 전부 그리되 투명도/샘플링으로 완화 — 안 B의 설명에는
   이 답이 없어 이전 세션에서 물었던 질문이 아직 살아있음
4. G(x) 곡선(안 A의 화면4)을 안 B의 3패널 레이아웃에도 넣을지 — 안 B는
   3패널만 언급하고 곡선 패널을 뺐음(스크롤 없이 3패널도 빠듯할 수 있어
   의도적으로 뺐을 가능성)
5. UI 파일 구조: CLAUDE.md 기본값(사용자 전역 설정)대로
   `index.html`/`style.css`/`script.js`로 나누고 `<script src="physics.js">`로
   더블클릭 실행 가능하게 만들면 되는지 확인 (ES 모듈·로컬 fetch 없이)
